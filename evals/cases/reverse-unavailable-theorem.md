# Trace a cited-theorem proof

Use `$reverse-lamport` to reconstruct the proof-supplied obligations within the declared source boundary.

## Theorem

Let `f : [0,1] → ℝ` be continuous, with `f(0) < 0` and `f(1) > 0`. Then there exists `c ∈ (0,1)` such that `f(c) = 0`.

## Submitted proof

The conclusion follows immediately from Theorem Z.

## Source boundary

The exact statement, hypotheses, and proof of Theorem Z are not supplied. Do not import an external theorem statement from memory.

## Accepted primitives

The stated hypotheses, the supplied function and interval notation, the definition of continuity, and elementary logic. No intermediate-value principle, zero-existence theorem, or content of Theorem Z is accepted as primitive.
